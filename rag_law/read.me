使用 环境 llamaindex-juke
在生成环境中使用 HuggingFace推理引擎进行推理是比较慢的 ， 我们要使用企业级的推理工具 vllm或者deploy进行推理

使用qwen1.8 llm推理：
根据劳动法第38条，劳动者可以解除合同的情形有哪些？ 但是给出的答复有 用人单位接入合同，
原因： 大模型对这个模型的理解不够
解决方法：
1. 对向量库的结果进行重排 2. 增大模型 3.微调 (有成本)

优先解决思路：
1. 对向量库检索出来的做一个重排序，让匹配度更高的排到前面进行过滤 （重排不一定能够排除）


向量库返回的余弦相似度都很高的原因：
1. 都是中文
2. 正确语义（语句连贯不是胡说八道） 这个和ai本身有关
一般 这个2个都符合的话，相似度会在0.7-0.8之间 ， 如果是乱码的话一般是0.4到0.6之间
但是我们一般检索相关要在0.9以上才有相关性
